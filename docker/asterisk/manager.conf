; Dev-only AMI config so the app's dialer engine has something to connect to
; locally. Production credentials live on the VPS, not in this repo.
[general]
enabled = yes
port = 5038
bindaddr = 0.0.0.0

[dialer]
secret = DialerAmi2026!
read = all
write = all
